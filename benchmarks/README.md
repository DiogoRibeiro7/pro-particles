# Benchmarks

This folder contains lightweight, reproducible performance comparisons for core methods.

## Run

```bash
python benchmarks/run.py
```

Outputs `results/benchmarks.json` with runtime, environment, and configuration notes.
