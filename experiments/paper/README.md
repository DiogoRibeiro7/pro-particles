# Paper-Ready Experiments

This folder contains paper-ready scripts that reproduce the main experiments.

## Run

```bash
python experiments/paper/run_all_paper.py
```

Outputs:
- `results/paper/paper_summary.json`

Notes:
- Uses full `config.json` for each experiment.
- See `src/pro_particles/experiments/datasets.py` for dataset loader helpers.
